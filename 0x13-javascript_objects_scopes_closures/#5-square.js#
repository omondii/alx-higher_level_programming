#!/usr/bin/node
/* clas Square inherits from Rectangle 4
   super() is caled within an inherited class to avoid duplicating
   the constructor parts shared btn square and rec
*/
class Square extends require('./4-rectangle.js') {
    constructor (size) {
	super(size, size)
    }
}

module.exports = Square;
